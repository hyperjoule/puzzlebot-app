import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "cover"
  },
  pieceContainer: {
    left: 0,
    overflow: "hidden",
    position: "absolute",
    top: 0
  }
});
