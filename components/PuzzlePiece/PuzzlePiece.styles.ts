import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "cover"
  },
  pieceContainer: {
    backgroundColor: "white",
    left: 0,
    overflow: "hidden",
    position: "absolute",
    top: 0
  }
});
